#include <iostream>
using namespace std;
int main(){
    int a=10,b=5;
    if(int c = a+b;c>a)
    {
    cout<<c<<endl;
    }
    //if i print c outside the if statement then it will show that the c is not defined
}